// Main test file that includes utils.cxy
#include "utils.cxy"

func main() {
    var result = add(10, 20);
    var circumference = 2 * PI * 5.0;

    globalCounter = globalCounter + 1;

    return result;
}

var mainVar = 42;
